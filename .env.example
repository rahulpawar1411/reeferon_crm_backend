# MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=reeferon_crm_db
DB_PORT=3306

# Optional: LOG_ALL_STATUS=1 to print every request status (default: only 4xx/5xx)
# LOG_ALL_STATUS=0

PORT=5000
JWT_SECRET=change_me_to_a_long_random_secret

# --- Email: Resend (required for local + deploy) ---
# https://resend.com/api-keys
RESEND_API_KEY=re_xxxx
EMAIL_FROM=ReeferON <onboarding@resend.dev>
APP_LOGIN_URL=http://localhost:3000

# Gmail SMTP (disabled — often blocked on Render)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=your.gmail@gmail.com
# SMTP_PASS=xxxx xxxx xxxx xxxx

# DO credentials email — app install + open
MOBILE_APP_URL=
MOBILE_DEEP_LINK=reeferon://login
