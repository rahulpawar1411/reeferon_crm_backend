# Railway: deploy this backend repo as a Web Service.
# Do NOT set PORT in Railway Variables — Railway injects it.

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
